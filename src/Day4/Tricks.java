package Day4;

public interface Tricks {
	//An interface is a declaration of methods, like a contract, between you and the class. 
	//any class that implements the interface must have the methods inside it.
	//if a class uses the interface then it must use the method. if a method is not being used, do not set any arguments.
	
	String doesTheseTricks();

}
